spring:
  application:
    name: svagarail-test-platform
  datasource:
    url: jdbc:h2:file:./data/svirail;MODE=PostgreSQL;AUTO_SERVER=TRUE
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
server:
  port: 8180
runner:
  enabled: false
  command: ""
  timeout-seconds: 600
